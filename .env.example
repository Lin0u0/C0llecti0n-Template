# Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# SECURITY - IMPORTANT!
# =============================================================================

# Admin API Authentication Key
# This key is required for all write operations (POST, PUT, DELETE) via the admin API.
# The admin panel will include this key in the X-Admin-Key header.
# 
# WARNING: Use a strong, random key in production!
# Generate a secure key with: openssl rand -base64 32
ADMIN_KEY=your-secret-admin-key-here

# =============================================================================
# Admin API Configuration (for local development)
# =============================================================================

# Admin API server URL
ADMIN_API_URL=http://localhost:4322/api

# Admin API server port (should match admin-server.mjs)
ADMIN_API_PORT=4322

# =============================================================================
# Security Notes
# =============================================================================
# 
# 1. CORS Protection:
#    The admin server only accepts requests from http://localhost:4321 by default.
#    Modify admin-server.mjs if you need to allow other origins.
#
# 2. XSS Protection:
#    All user inputs are sanitized before rendering. The escapeHtml() function
#    is used throughout the application to prevent XSS attacks.
#
# 3. Authentication:
#    - Read operations (GET) don't require authentication
#    - Write operations (POST, PUT, DELETE) require the X-Admin-Key header
#
# 4. Deployment:
#    - Never commit your actual .env file
#    - Use strong, unique keys for production
#    - Consider adding additional authentication layers for production admin access
